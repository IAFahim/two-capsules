The property you assign is generated, and its setter is hard wired to a helper that compares the old value with the new one and returns immediately if they match. So an idle screen with fifty bound fields, written every frame, costs fifty comparisons and nothing else. No allocation, no delegate, no boundary crossing. And because the setter is generated, there is no way to write the field and skip the check, which is exactly the property you want from a mechanism like this.