The helper resolves the view model from a service container, pins it, and caches the address. It can also build a query so the system only updates while the relevant screen is the active destination, which is how a view model stops costing anything the moment its screen is gone.