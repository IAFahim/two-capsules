The hard part is per-connection baselines. Every client has ACKed a different tick, so the server must remember what each one knows and delta against that. Get it wrong and clients decode garbage, intermittently, under loss only.