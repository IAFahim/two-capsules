The rule underneath all of it: if a client can state it, a client can lie about it, so make the server derive it instead. Chapter 31's table F is the same rule as a design question rather than a threat.