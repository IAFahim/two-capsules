There is no load function you call and no handle you keep. There is a component. Add it to a scene entity and the section streams in. Remove it and the section streams out. That is the whole public interface, and it means loading is a piece of data rather than a piece of control flow, which in turn means the load state of your world is something a system can query, save and replicate like anything else.