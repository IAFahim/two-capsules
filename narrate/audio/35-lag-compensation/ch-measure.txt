The server does not estimate how far behind a client is rendering. The client measures it, as the number of ticks between the server tick and its own interpolation tick, adjusted by the sub tick fractions and floored at zero. That number is written into every command packet, stored per connection on the server, and copied onto the owned ghost when a command for that ghost arrives.