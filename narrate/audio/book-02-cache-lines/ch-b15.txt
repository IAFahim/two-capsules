Trap, adding or removing a component moves the entity to a different chunk. That is a memcpy of every component plus bookkeeping, and it invalidates any raw pointer you were holding. This is why DOTS code obsessively defers structural changes into an Entity Command Buffer and plays it back at a known safe point. It is the same discipline as "don't free() inside the interrupt handler".