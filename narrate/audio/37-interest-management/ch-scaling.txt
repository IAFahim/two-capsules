Before the sort, the server hands the whole list of chunks to a function pointer you supply. The built in one is distance based: it takes the difference between the chunk's tile and the player's tile, squares the length, and divides priority by it. Anything within one tile in any direction is treated as here, because a player standing near a tile edge must not lose the tile they are looking at.