Doing this in code rather than by hand in the graph window is the difference between a reproducible project and an artifact nobody dares regenerate. Nerve's own sample builds its graphs this way; so does ours (Assets/Editor/Client Graph Setup.cs).