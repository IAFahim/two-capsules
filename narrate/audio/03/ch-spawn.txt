With the connection approved and in game, the session system spawns two entities per player: a controller, which is the persistent identity, and a controlled pawn, which is the current body. Snapshots begin, and the ghosts materialise on the client without the client ever calling instantiate. Two players should therefore show four ghosts. If you see three, two connections adopted the same controller.