Here is the thing worth internalising. It does not complete the jobs that touch this archetype. It does not complete the jobs that touch these component types. It walks every type that has a fence registered, gathers the write fence and every read fence of each one, combines them all into a single handle, and waits on that. Then it forgets every fence it was tracking, so the whole dependency graph restarts from empty. One entity gaining one tag drains the entire world.