So the lookup is: mask the index, read the slot, compare the version. Equal, and you get a chunk and a slot number unpacked from the same word you already loaded. Not equal, and you get nothing. There is no third branch where it half works. A stale handle in this system is a null, not a corrupted read, and that difference is the whole reason you can hand entity handles around inside components without fear.