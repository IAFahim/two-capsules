Every block inside the gate reports whether it is completed, and the gate advances only when all of them say yes. So while the connection has no network id, the gate simply holds. When the request has been sent but the server has not confirmed, it holds. No timers, no coroutines, no retry counters. The barrier is the retry loop, and reaching gameplay is proof the whole chain worked.