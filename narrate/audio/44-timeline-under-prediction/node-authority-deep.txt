The cleanest gate in the set, because it is enforced at bake time rather than at runtime. Marking the component as server only means the client prefab never receives it, so there is no branch to forget and no flag to check. If a client could not run it even in principle, a client cannot get it wrong.