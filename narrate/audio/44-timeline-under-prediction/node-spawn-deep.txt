The client marks its speculative spawn with a predicted spawn request. When the authoritative version arrives, a classification system matches the two instead of creating a second, and any predicted spawn that never gets matched is destroyed once it ages out. So duplicates are reconciled rather than prevented, which is the only thing that works when existence itself is never rewound.