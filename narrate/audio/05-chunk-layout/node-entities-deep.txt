An entity handle is an index plus a version number. The version is the trick that makes a stale reference detectable instead of catastrophic: reuse the index, bump the version, and any handle carrying the old version now fails a cheap comparison instead of silently addressing somebody else's data.