Native code cannot call a managed method. So how does compiled code inside the command buffer reach the managed part of playback? The generated bridge is lovely. There is a small method marked so that Burst deletes its body from the native build. In managed code it sets a flag to true. In native code the call vanishes and the flag stays false. Calling a method and observing that nothing happened is how the code works out which world it is running in.