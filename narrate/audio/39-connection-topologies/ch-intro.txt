People collapse four separate decisions into one word and then argue about it. Topology is who owns the authoritative simulation. The driver store is which driver instances exist. The interface is how bytes leave the process. Reachability is whether the client can address the host at all. A relay answers the fourth question and changes nothing about the first.