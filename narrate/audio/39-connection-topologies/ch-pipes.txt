Each driver creates three pipelines. Unreliable sequenced carries snapshots and commands. Reliable sequenced carries remote procedure calls. A fragmenting pipeline carries snapshots too big for one message. In the editor, with the simulator on, a fourth stage is appended to all three on the client only. Each stage adds header bytes, and the send system subtracts them before deciding how big a snapshot may be.