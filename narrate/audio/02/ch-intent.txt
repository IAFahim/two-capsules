The gather system reads the keyboard and writes a move direction onto your capsule, and only yours. It filters on ghost owner is local, so it cannot accidentally drive another player. Notice what it does not write. It does not write a position, a velocity, or a final answer. A direction is bounded; the server applies its own speed and its own delta to it, so a modified client gains nothing.