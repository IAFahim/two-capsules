Nerve is a bootstrap in its own right and separately extends the netcode one, which is exactly the inheritance trick the scan enables. It constructs a persistent service world from a filter bit of its own, then hand picks the Unity scene and entity systems it wants alongside. Client and server worlds are created and destroyed later, on a state graph transition. Netcode decides whether those worlds exist. Nerve decides when.